{
  "name": "presentation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preview": "marp --preview --watch --output out/ --input-dir slides/",
		"presentation": "marp --preview --bespoke.progress=true --bespoke.osc=true --output out/ --input-dir slides/",
		"export:pdf": "marp --pdf --output pdf/ --input-dir slides/ --allow-local-files",
		"export:notes": "marp --notes --output out/ --input-dir slides/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^2.1.2"
  }
}
